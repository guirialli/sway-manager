[package]
name = "sway-manager-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sway-manager-cli"
path = "src/main.rs"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["net", "io-util", "rt-multi-thread", "macros", "time"] }
dirs = "5.0"
